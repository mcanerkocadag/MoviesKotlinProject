<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools" xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android" android:id="@+id/layout3">
    <data>
        <variable name="movie" type="com.example.movies.network.movies.Result"/>
        <variable name="listener" type="com.example.movies.ui.movies.MovieRecyclerviewAdapter.MovieListener"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:layout_gravity="center"
            android:layout_marginBottom="4dp"
            android:onClick="@{()->listener.onClick(movie)}"
            android:gravity="center">

        <TextView
                android:text="@{movie.title}"
                android:layout_height="wrap_content"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:id="@+id/sube_kodu"
                android:gravity="left"
                android:layout_gravity="center"
                tools:text="1342-6676239"
                android:textSize="18sp"
                android:layout_marginEnd="8dp"
                android:layout_marginRight="8dp"
                android:layout_marginStart="8dp"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginLeft="8dp"
                app:layout_constraintHorizontal_bias="1.0" android:layout_marginTop="8dp"
                app:layout_constraintTop_toTopOf="parent" app:layout_constraintEnd_toEndOf="parent"/>


        <TextView
                android:layout_width="0dp"
                android:layout_weight="3"
                android:layout_height="wrap_content"
                android:id="@+id/sube_bakiye"
                android:gravity="center"
                android:layout_gravity="center"
                android:textSize="30sp"
                app:layout_constraintStart_toStartOf="@+id/sube_kodu"
                android:layout_marginTop="8dp"
                app:layout_constraintTop_toBottomOf="@+id/sube_kodu" android:layout_marginBottom="8dp"
                app:layout_constraintBottom_toBottomOf="parent"/>
        <ImageView
                android:layout_width="0dp"
                android:layout_height="0dp" tools:srcCompat="@tools:sample/avatars[2]"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" android:id="@+id/imageView4"
                app:layout_constraintStart_toStartOf="parent" app:layout_constraintEnd_toEndOf="parent"
                android:scaleType="fitXY"
                app:imageUrl="@{movie.poster_path}"
                android:layout_marginEnd="8dp" android:layout_marginRight="8dp"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>